spring.application.name=RapidTicketAccess

server.port=${PORT:8080}

#Security Credentials
spring.security.user.name=aravinth
spring.security.user.password=kumar

# Database Configuration

#spring.datasource.url=jdbc:mysql://localhost:3306/rapid_ticket_access
#spring.datasource.username=root
#spring.datasource.password=root
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Standard Railway MySQL connection
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true